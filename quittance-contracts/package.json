{
  "name": "quittance-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network kite_testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network kite_mainnet",
    "verify:testnet": "hardhat run scripts/verify.ts --network kite_testnet",
    "seed:testnet": "hardhat run scripts/seed.ts --network kite_testnet",
    "typechain": "hardhat typechain",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.0"
  }
}
